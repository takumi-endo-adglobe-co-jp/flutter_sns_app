# Flutter SNS App Specification

## プロジェクト概要
FlutterとSupabase(PostgreSQL)を使用して、X（Twitterのような）テキストベースのSNSライクなアプリを作成する。  
1画面で実装する。

プロジェクトのアーキテクチャ構成はdocs/architecture.mdを遵守する。

## 機能要件

### 1. 投稿の表示機能
- 1度に表示される投稿数は10件
- 最下部に移動したら、さらに10件読み込まれ表示（無限スクロール）

### 2. 投稿機能
- 0文字で投稿は不可
- 512文字まで入力可能

### 3. 投稿編集機能
- 投稿の文章部分がテキストボックスに変化し、自由に編集可能
- 保存ボタンとキャンセルボタンを新たに表示
- 保存ボタンを押下すると、編集した文章に変更
- キャンセルボタンを押下すると、元の文章のままにする
- 0文字に編集した場合は投稿不可

### 4. 投稿削除機能
- 削除時、確認のポップアップ表示

### 5. 投稿へのいいね機能
- トグル式（いいねのオン/オフ切り替え）

## UI/UX
UI/UXはお任せする。